<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup</title>
    <link href="/css/tailwind.css" rel="stylesheet">
    <script async src="https://analytics.vercel.app/"></script>
</head>

<body class="bg-[#f8f5f2] text-[#4a4845] h-screen">

    <div class="flex items-center justify-center max-h-screen min-h-full p-6">

        <!-- Signup Form Container -->
        <div class="bg-white p-8 rounded-lg shadow-xl w-full max-w-md border border-[#eae6e3]">
            <h2 class="text-2xl font-bold text-center text-[#4a4845] mb-6">Create an Account</h2>

            <!-- Display Error Message if exists -->
            <% if (locals.error) { %>
                <div class="mb-4 text-red-500 text-sm text-center">
                    <%= locals.error %>
                </div>
            <% } %>

            <!-- Signup Form -->
            <form action="/user/signup" method="POST">

                <!-- Username Input -->
                <div class="mb-6">
                    <label for="username" class="block text-sm font-medium text-[#4a4845]">Username</label>
                    <input type="text" id="username" name="username"
                        class="w-full mt-2 px-4 py-2 border-b-2 border-[#eae6e3] bg-transparent text-[#4a4845] rounded-md focus:outline-none focus:border-[#d9d4cc] transition"
                        required
                        <% if (locals.username) { %>
                        value="<%= username %>"
                        <% } %>
                    >
                </div>

                <!-- Password Input -->
                <div class="mb-6">
                    <label for="password" class="block text-sm font-medium text-[#4a4845]">Password</label>
                    <input type="password" id="password" name="password"
                        class="w-full mt-2 px-4 py-2 border-b-2 border-[#eae6e3] bg-transparent text-[#4a4845] rounded-md focus:outline-none focus:border-[#d9d4cc] transition"
                        required
                        <% if (locals.password) { %>
                        value="<%= locals.password %>"
                        <% } %>
                    >
                </div>

                <!-- Submit Button -->
                <div class="mb-6">
                    <button type="submit"
                        class="w-full bg-[#e3dfda] text-[#4a4845] font-bold py-2 rounded-md hover:bg-[#d9d4cc] focus:outline-none focus:ring-2 focus:ring-[#d9d4cc] transition">
                        Sign Up
                    </button>
                </div>
            </form>

            <!-- Login Link -->
            <p class="text-center text-sm text-[#6e7f7f]">
                Already have an account? <a href="/user/signin"
                    class="text-[#4a4845] hover:underline">Login here</a>.
            </p>
        </div>
    </div>

</body>

</html>
