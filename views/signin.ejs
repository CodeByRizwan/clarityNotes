<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signin</title>
    <link href="/css/tailwind.css" rel="stylesheet">
</head>

<body class="bg-[#f8f5f2] text-[#4a4845] flex items-center justify-center h-screen px-6">

    <!-- Signup Form Container -->
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md border border-[#eae6e3]">
        <h2 class="text-2xl font-bold text-center text-[#4a4845] mb-6">Sign in to your account</h2>

        <!-- Display Error Message if exists -->
        <% if (locals.error) { %>
            <div class="mb-4 text-red-500 text-sm text-center">
                <%= locals.error %>
            </div>
        <% } %>

        <!-- Signup Form -->
        <form action="/user/signin" method="POST">
            <!-- Username Input -->
            <div class="mb-6">
                <label for="username" class="block text-sm font-medium text-[#4a4845]">Username</label>
                <input type="text" id="username" name="username" class="w-full mt-2 px-4 py-2 border-b-2 border-[#eae6e3] bg-transparent text-[#4a4845] rounded-md focus:outline-none focus:border-[#d9d4cc] transition"
                    required
                    <% if (locals.username) { %>
                        value="<%= username %>"
                    <% } %>
                >
            </div>

            <!-- Password Input -->
            <div class="mb-6">
                <label for="password" class="block text-sm font-medium text-[#4a4845]">Password</label>
                <input type="password" id="password" name="password" class="w-full mt-2 px-4 py-2 border-b-2 border-[#eae6e3] bg-transparent text-[#4a4845] rounded-md focus:outline-none focus:border-[#d9d4cc] transition"
                    required
                    <% if (locals.password) { %>
                        value="<%= locals.password %>"
                    <% } %>
                >
            </div>

            <!-- Submit Button -->
            <div class="mb-6">
                <button type="submit"
                    class="w-full bg-[#e3dfda] text-[#4a4845] font-bold py-2 rounded-md hover:bg-[#d9d4cc] focus:outline-none focus:ring-2 focus:ring-[#d9d4cc] transition">
                    Sign in
                </button>
            </div>
        </form>

        <!-- Signup Link -->
        <p class="text-center text-sm text-[#6e7f7f]">
            Do not have an account? <a href="/user/signup" class="text-[#4a4845] hover:underline">Sign up here</a>.
        </p>
    </div>

</body>

</html>
