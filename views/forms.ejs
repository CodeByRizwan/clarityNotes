<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Book</title>
    <link href="/css/tailwind.css" rel="stylesheet">
</head>
<body>
    <% if (whatForm === "book") { %>
        <div id="modal" class="fixed inset-0 bg-black bg-opacity-30 items-center justify-center flex z-50">
            <div class="bg-[#ffffff] w-4/5 max-w-md rounded-lg p-6 shadow-lg border border-[#eae6e3]">
                <h2 class="text-xl font-bold text-[#4a4845] mb-4">Add a New Book</h2>
                <form action="/home/add-book" method="POST" >
                    <div class="mb-4">
                        <label for="name" class="block text-sm font-medium text-[#817e79]">Book Name</label>
                        <input type="text" name="name" id="name" class="w-full border-b border-[#eae6e3] bg-transparent text-[#4a4845] px-2 py-2 focus:outline-none focus:border-[#d9d4cc] transition" placeholder="Enter book name" required>
                    </div>
                    <div class="mb-4">
                        <label for="author" class="block text-sm font-medium text-[#817e79]">Author</label>
                        <input type="text" name="author" id="author" class="w-full border-b border-[#eae6e3] bg-transparent text-[#4a4845] px-2 py-2 focus:outline-none focus:border-[#d9d4cc] transition" placeholder="Enter author name" required>
                    </div>
                    <div class="flex justify-end space-x-4">
                        <button type="submit" class="bg-[#e3dfda] text-[#4a4845] px-4 py-2 rounded-md hover:bg-[#d9d4cc] transition">Add Book</button>
                    </div>
                </form>
            </div>
        </div>
    <% } %>

    <% if (whatForm === "chapter") { %>
        <div id="modal" class="fixed inset-0 bg-black bg-opacity-30 flex items-center justify-center">
            <div class="bg-[#ffffff] w-4/5 max-w-md rounded-lg p-6 shadow-lg border border-[#eae6e3]">
                <h2 class="text-xl font-bold text-[#4a4845] mb-4">Add a New Chapter</h2>
                <form action="/add/<%= bookId %>/chapter" method="POST">
                    <div class="mb-4">
                        <label for="name" class="block text-sm font-medium text-[#817e79]">Chapter Name</label>
                        <input
                            type="text"
                            name="name"
                            id="name"
                            class="w-full border-b border-[#eae6e3] bg-transparent text-[#4a4845] px-2 py-2 focus:outline-none focus:border-[#d9d4cc] transition"
                            placeholder="Enter chapter name"
                            required>
                    </div>
                    <div class="mb-4">
                        <label for="number" class="block text-sm font-medium text-[#817e79]">Chapter Number</label>
                        <input
                            type="number"
                            name="number"
                            id="number"
                            class="w-full border-b border-[#eae6e3] bg-transparent text-[#4a4845] px-2 py-2 focus:outline-none focus:border-[#d9d4cc] transition"
                            placeholder="Enter chapter number"
                            required>
                    </div>
                    <div class="flex justify-end space-x-4">
                        <button
                            type="submit"
                            class="bg-[#e3dfda] text-[#4a4845] px-4 py-2 rounded-md hover:bg-[#d9d4cc] transition">
                            Add Chapter
                        </button>
                    </div>
                </form>
            </div>
        </div>
    <% } %>

    <% if (whatForm === "field") { %>
        <div id="modal" class="fixed inset-0 bg-black bg-opacity-50 items-center justify-center flex">
            <div class="bg-[#ffffff] w-4/5 max-w-md rounded-lg p-6 shadow-lg border border-[#eae6e3]">
                <h2 class="text-lg font-semibold text-[#4a4845] mb-4">Add Custom Field</h2>
                <form action="/add/<%= chapterId %>/field" method="POST">
                    <div class="mb-4">
                        <label for="fieldName" class="block text-sm font-medium text-[#817e79]">Field Name</label>
                        <input type="text" name="fieldName" id="fieldName"
                            class="w-full border-b border-[#eae6e3] bg-transparent text-[#4a4845] px-2 py-2 focus:outline-none focus:border-[#d9d4cc] transition"
                            required>
                    </div>
                    <div class="flex justify-end space-x-4">
                        <button type="submit"
                            class="bg-[#e3dfda] text-[#4a4845] px-4 py-2 rounded-md hover:bg-[#d9d4cc] transition">
                            Add Field
                        </button>
                    </div>
                </form>
            </div>
        </div> 
    <% } %>

</body>
</html>